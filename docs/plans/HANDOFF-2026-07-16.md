# Handoff — 2026-07-16

##瓶颈（去 DIT_HAP_snakemake 对话解决）

`HD_DIT_HAP_generationRAW` 没打包 release/。本项目所有正式分析都要在 HD 上做,但
`/data/c/yangyusheng_optimized/DIT_HAP_snakemake/projects/HD_DIT_HAP_generationRAW/release/` 不存在。

**要做的**:把 HD 打包出 `release/gene_level/fitting_results.tsv`。先查为什么没跑(缺数据?
packaging 配置没写?)。LD_DIT_HAP_generationRAW 已打包成功,可对比它的配置。

HD 好了之后:
```
snakemake --use-conda results/clustering/candidates/HD_DIT_HAP_generationRAW/candidate_clusters.tsv
# 然后跑 notebooks/clustering/finalize_gene_clusters.ipynb（DATASET=HD），肉眼合并 64→9 簇
# → 生成 resources/curated/final_clusters.tsv
# 再跑 enrichment / ml（都依赖 final_clusters.tsv）
```

## 进度

- **Phase 1**:完成并提交（feature collection 全链路）。
- **Phase 2（核心链 clustering→enrichment→ml）:全部 8 任务完成并提交。** 见
  `2026-07-16-DIT-HAP-analysis-phase2-implementation.md`。79 个测试通过。
  - clustering.smk / enrichment.smk / enrichment_network.smk / ml.smk 都已接入 Snakefile
  - 所有确定性脚本已用 LD 数据端到端验证逻辑
  - **人工环节**:`notebooks/clustering/finalize_gene_clusters.ipynb`（合并 64→9 簇）
    是唯一非自动步骤,产出 `resources/curated/final_clusters.tsv`
- **未做（Phase 3+）**:其余 8 个确定性阶段（coverage / verification / comparison /
  noncoding_rna / spikein / complex / utr / domain_differences）。

## 关键约束

- **一次性 focus HD**：正式分析只在 HD 上做。LD 只用于验证代码逻辑,产出丢弃。
- 代码保留多 dataset 通用能力（{dataset} wildcard），但人工调参/合并只针对 HD。
- 测试用 `conda run -n bioinformatics pytest`,Snakemake 用 `mamba run -n snakemake`。
