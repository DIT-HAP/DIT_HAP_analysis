# Handoff — 2026-07-16

## 瓶颈（去 DIT_HAP_snakemake 对话解决）

`HD_DIT_HAP_generationRAW` 没打包 release/。本项目所有正式分析都要在 HD 上做,但
`/data/c/yangyusheng_optimized/DIT_HAP_snakemake/projects/HD_DIT_HAP_generationRAW/release/` 不存在。

**要做的**:把 HD 打包出 `release/gene_level/fitting_results.tsv`。先查为什么没跑(缺数据?
packaging 配置没写?)。LD_DIT_HAP_generationRAW 已打包成功,可对比它的配置。

HD 好了之后本项目直接切:`snakemake ... results/clustering/candidates/HD_DIT_HAP_generationRAW/...`,不用改代码。

## 当前进度

- Phase 1:完成并提交(9 commits)。
- Phase 2 计划:见 `2026-07-16-DIT-HAP-analysis-phase2-implementation.md`(8 任务)。
- **Task 1 (clustering):代码+7 测试完成、LD 跑通,但未 commit。** 剩:确认
  `workflow/envs/statistics_and_figure_plotting.yml` 里有 scikit-learn(被打断时正要加),
  跑全套 pytest,然后 commit。
- Task 2-8:未开始。

## 未提交改动

新增 `workflow/scripts/clustering/generate_candidate_clusters.py`、`workflow/rules/clustering.smk`、
`tests/test_clustering.py`;改 `Snakefile`、`config/analysis.yaml`、`statistics_and_figure_plotting.yml`。

## 注意

- 测试用 `conda run -n bioinformatics pytest`,Snakemake 用 `mamba run -n snakemake`(两个 env 互相看不见)。
- 当前 LD 只验证逻辑,不产正式结果——正式分析等 HD。
